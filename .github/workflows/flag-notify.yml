name: Notify Inconsistency Flags
on:
  schedule:
    - cron: "30 0 * * 1-5"  # 매일 오전 9시 30분(KST), 평일만
  workflow_dispatch: {}

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - run: python3 scripts/notify_flags.py
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_WBS_DB_ID: ${{ secrets.NOTION_WBS_DB_ID }}
          NOTION_APISPEC_DB_ID: ${{ secrets.NOTION_APISPEC_DB_ID }}
          NOTION_FUNCSPEC_DB_ID: ${{ secrets.NOTION_FUNCSPEC_DB_ID }}
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}